需要以下操作才能显示效果：
1. 首先电脑需安装 node.js(网址：https://nodejs.org/en)。
2. 打开 01-cookie或 02-session 文件夹，然后打开终端，输入命令 npm install 下载依赖包。
3.  打开文件夹下的 db.js 文件，进行 mysql 的配置，便且创建相应的数据库。
4. 然后输入命令 node ./app.js 运行。
5. 终端不关闭，打开浏览器输入 http://127.0.0.1:81 即可。